import { IfMediaQueryDirectiveDirective } from './media.directive';

describe('IfMediaQueryDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IfMediaQueryDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
